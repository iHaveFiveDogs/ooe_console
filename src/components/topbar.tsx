// Deprecated legacy topbar component.
// This file was retained as a stub to avoid accidental imports; the canonical Topbar is in `Topbar.tsx`.
import React from 'react'

export default function DeprecatedTopbar() {
    if (typeof console !== 'undefined') console.warn('[DeprecatedTopbar] legacy topbar.tsx imported. Use components/Topbar.tsx instead.')
    return null
}
